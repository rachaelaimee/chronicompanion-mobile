[phases.setup]
nixPkgs = ["python3", "gcc", "pkg-config", "postgresql", "libffi", "openssl"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip", "python3 -m pip install -r requirements.txt"]

[start]
cmd = "python main.py"